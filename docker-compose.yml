version: "3"
services:
  frontend:
    tty: true
    build:
      context: ./my-app/
      dockerfile: Dockerfile
    image: frontend:latest
    ports:
      - ${FRONTEND_PORT}:3000
    volumes:
      - /usr/src/app/node_modules
      - ./my-app:/usr/src/app/
